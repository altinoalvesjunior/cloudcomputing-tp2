apiVersion: v1
kind: Service
metadata:
  name: playlist-recommender-service
  namespace: altinojunior
spec:
  selector:
    app: playlist-recommender
  type: NodePort
  ports:
    - protocol: TCP
      port: 5000
      targetPort: 5000
      nodePort: 30502