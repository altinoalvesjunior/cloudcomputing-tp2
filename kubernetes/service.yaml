apiVersion: v1
kind: Service
metadata:
  name: playlist-recommender-service
  namespace: altinojunior
spec:
  selector:
    app: playlist-recommender
  type: NodePort
  ports:
    - protocol: TCP
      port: 9101
      targetPort: 9101
      nodePort: 30502